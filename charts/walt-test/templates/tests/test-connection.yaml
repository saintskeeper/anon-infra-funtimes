apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "walt-test.fullname" . }}-test-connection"
  labels:
    {{- include "walt-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "walt-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
